
const store={save:(k,v)=>localStorage.setItem(k,JSON.stringify(v)),get:(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch(e){return f}}};
if(!store.get('athletes')){store.save('athletes',[{name:'Marcos Silva',modality:'Muay Thai',goal:'Performance',level:'Intermediário',weight:'72',notes:'Sem lesões'},{name:'Ana Rocha',modality:'Hipertrofia',goal:'Ganho de massa',level:'Iniciante',weight:'58',notes:'Dor leve no joelho'}]);}
function renderAthletes(id){const el=document.getElementById(id);if(!el)return;const a=store.get('athletes',[]);el.innerHTML=a.map(x=>`<div class="item"><div class="statline"><strong>${x.name}</strong><span class="small">${x.modality}</span></div><div class="small">${x.goal} • ${x.level} • ${x.weight} kg</div></div>`).join('')}
function addAthlete(id){const f=document.getElementById(id);if(!f)return;f.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(f).entries());const a=store.get('athletes',[]);a.push(data);store.save('athletes',a);alert('Atleta salvo no protótipo.');f.reset();renderAthletes('athleteList')})}
function aiGenerate(){const prompt=document.getElementById('prompt');const out=document.getElementById('aiOut');if(!prompt||!out||!prompt.value.trim())return;out.innerHTML=`<div class="msg user">${prompt.value}</div><div class="msg ai"><strong>Sugestão IC Fitness AI</strong><br><br>1. Aplicar testes: mobilidade, resistência específica, força básica e VAM.<br>2. Dividir em 3 blocos: adaptação, desenvolvimento e refinamento.<br>3. Estrutura semanal inicial:<br>• Força/hipertrofia: 2-3x<br>• Técnica/específico: 2-4x<br>• Aeróbio/intervalado: 1-2x<br>• Recuperação ativa: 1x<br><br><strong>Treino exemplo editável</strong><br>A) Agachamento 4x6-8<br>B) Supino 4x6-8<br>C) Remada 4x8-10<br>D) Core 3x30-40s<br>E) Intervalado 8x30'' / 60''<br><br><span class="small">Baseado em princípios gerais de sobrecarga progressiva, especificidade, individualização e controle de carga. No produto final, isso poderá virar PDF, Excel e treino salvo no app.</span></div>`}
